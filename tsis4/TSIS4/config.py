# Конфигурационный файл игры "Змейка"
# Все константы и настройки игры

# ─── Размеры окна ───────────────────────────────────────────────────────────────
WINDOW_WIDTH  = 800   # Ширина окна в пикселях
WINDOW_HEIGHT = 650   # Высота окна в пикселях

# ─── Параметры сетки ────────────────────────────────────────────────────────────
GRID_SIZE = 20                              # Размер одной клетки в пикселях
COLS      = WINDOW_WIDTH // GRID_SIZE       # Количество столбцов сетки (40)
ROWS      = (WINDOW_HEIGHT - 50) // GRID_SIZE  # Количество строк сетки (30, 50px под HUD)

# ─── Скорость игры ──────────────────────────────────────────────────────────────
FPS_BASE = 10  # Базовая скорость (кадров в секунду)

# ─── Цветовая палитра ───────────────────────────────────────────────────────────
BLACK      = (0,   0,   0)
WHITE      = (255, 255, 255)
GREEN      = (0,   200, 0)
DARK_GREEN = (0,   140, 0)
RED        = (220, 20,  20)
DARK_RED   = (160, 0,   0)
BLUE       = (30,  100, 220)
YELLOW     = (255, 220, 0)
ORANGE     = (255, 140, 0)
PURPLE     = (160, 0,   200)
GRAY       = (160, 160, 160)
DARK_GRAY  = (60,  60,  60)

# ─── Параметры базы данных ──────────────────────────────────────────────────────
DB_HOST     = "localhost"  # Хост PostgreSQL
DB_PORT     = 5432         # Порт PostgreSQL
DB_NAME     = "snake_db"   # Имя базы данных
DB_USER     = "postgres"   # Имя пользователя
DB_PASSWORD = "postgres"   # Пароль

# ─── Типы еды и их веса ─────────────────────────────────────────────────────────
FOOD_WEIGHTS = {
    'normal': 1,  # Обычная еда — 1 очко
    'bonus':  3,  # Бонусная еда — 3 очка
    'super':  5,  # Супер-еда   — 5 очков
}

# ─── Типы бонусов (power-up) ────────────────────────────────────────────────────
POWERUP_TYPES = ['speed', 'slow', 'shield']
# speed  — ускорение змейки
# slow   — замедление змейки
# shield — щит: поглощает одно столкновение

# ─── Прогрессия уровней ─────────────────────────────────────────────────────────
LEVEL_THRESHOLD = 5  # Количество съеденных единиц еды для перехода на следующий уровень
